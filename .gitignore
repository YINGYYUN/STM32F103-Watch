# ===================== Keil MDK 专用忽略规则 =====================
# 1. 编译中间文件（无提交价值，编译时自动生成）
*.o          # 目标文件
*.d          # 依赖文件
*.axf        # 可执行文件（调试用）
*.hex        # 烧录文件（可按需取消忽略，若团队需同步烧录包）
*.bin        # 二进制烧录文件（同上）
*.lst        # 列表文件
*.map        # 链接映射文件
*.htm        # 编译报告文件
*.dep        # 依赖描述文件

# 2. Keil 自动生成的目录（编译产物存放）
Objects/     # 目标文件目录
Listings/    # 列表文件目录

# 3. 本地 GUI 配置文件（每个人的本地状态，必忽略，避免冲突）
*.uvguix.*   # Keil 窗口布局/光标位置等本地配置（如Project.uvguix.HP）

# 4. 临时/备份文件（自动生成，无意义）
*.log        # 日志文件
*.bak        # 备份文件
*.tmp        # 临时文件
*.lnp        # Keil 路径配置临时文件（补充：易被忽略的临时文件）

# 5. 可选：若团队不需要同步 Keil 调试配置，可添加（谨慎使用）
# *.uvoptx    # 工程选项文件（若改了编译配置需提交，建议不忽略）
# *.uvproj    # 工程核心文件（必保留，仅忽略GUI相关）